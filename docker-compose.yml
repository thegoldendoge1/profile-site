version: '3.8'

services:
  landing:
    image: nginx:alpine
    container_name: lenscape-landing
    restart: always
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8081:80"
